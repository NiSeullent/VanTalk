# local session / chat DBs (never commit)
login_data.json
last_tokens.json
**/login_data.json
**/*.db
**/*.sqlite

# OS junk
.DS_Store
Thumbs.db
*.log

# release artifacts (uploaded via gh release, not git)
*.zip
linux/
windows/

# accidental source drops
src/
*.java
.gradle/
build/
