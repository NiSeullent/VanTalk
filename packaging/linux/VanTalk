#!/usr/bin/env bash
set -euo pipefail
URL="${VANTALK_APP_URL:-https://vantalk.nyase.kr/}"
if command -v xdg-open >/dev/null; then xdg-open "$URL"
elif command -v gio >/dev/null; then gio open "$URL"
else echo "Open $URL in your browser"; fi
